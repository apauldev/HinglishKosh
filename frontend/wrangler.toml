name = "hinglishkosh"
pages_build_output_dir = "public"

# D1 binding — configure in Cloudflare Pages dashboard:
#   Dashboard → hinglishkosh → Settings → Functions → D1 Database Bindings
#   Variable name: DB
#   D1 Database: hinglishkosh
#
# For local dev, set via env:
#   export CLOUDFLARE_D1_ID="your-database-uuid"
